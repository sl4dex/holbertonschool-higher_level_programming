def print_matrix_integer(matrix=[[]]):
    if matrix is None:
        print()
    else:
    for row in matrix:
        for elem in row:
            print("{}".format(elem))
		    if elem 
        print()

